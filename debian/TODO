Here are the parts missing from this package:

 1. setup sudo (mostly done: need to warn the user if #includedir not
    set)
 2. comply with the FHS
 3. prompt the user for the DB credentials (db-common?)
 4. install hostmaster itself
 5. hook into the apache config and restart apache
